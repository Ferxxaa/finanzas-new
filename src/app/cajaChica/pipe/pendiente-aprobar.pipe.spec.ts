import { PendienteAprobarPipe } from './pendiente-aprobar.pipe';

describe('PendienteAprobarPipe', () => {
  it('create an instance', () => {
    const pipe = new PendienteAprobarPipe();
    expect(pipe).toBeTruthy();
  });
});
