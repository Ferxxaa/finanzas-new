import { CalcSaldoPipe } from './calc-saldo.pipe';

describe('CalcSaldoPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcSaldoPipe();
    expect(pipe).toBeTruthy();
  });
});
