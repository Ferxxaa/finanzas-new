import { ResponsableNamePipe } from './responsable-name.pipe';

describe('ResponsableNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsableNamePipe();
    expect(pipe).toBeTruthy();
  });
});
