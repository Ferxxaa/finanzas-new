import { CorrelativoPipe } from './correlativo.pipe';

describe('CorrelativoPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrelativoPipe();
    expect(pipe).toBeTruthy();
  });
});
