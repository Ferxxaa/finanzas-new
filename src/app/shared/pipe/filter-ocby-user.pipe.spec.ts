import { FilterOcbyUserPipe } from './filter-ocby-user.pipe';

describe('FilterOcbyUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOcbyUserPipe();
    expect(pipe).toBeTruthy();
  });
});
