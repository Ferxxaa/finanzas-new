import { ComatopointPipe } from './comatopoint.pipe';

describe('ComatopointPipe', () => {
  it('create an instance', () => {
    const pipe = new ComatopointPipe();
    expect(pipe).toBeTruthy();
  });
});
