import { SortOcDescPipe } from './sort-oc-desc.pipe';

describe('SortOcDescPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOcDescPipe();
    expect(pipe).toBeTruthy();
  });
});
