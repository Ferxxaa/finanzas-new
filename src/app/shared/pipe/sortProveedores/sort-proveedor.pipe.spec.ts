import { SortProveedorPipe } from './sort-proveedor.pipe';

describe('SortProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
