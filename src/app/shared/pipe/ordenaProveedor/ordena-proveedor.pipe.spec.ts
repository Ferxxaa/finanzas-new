import { OrdenaProveedorPipe } from './ordena-proveedor.pipe';

describe('OrdenaProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenaProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
