import { FilterCentroCostoActivePipe } from './filter-centro-costo-active.pipe';

describe('FilterCentroCostoActivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCentroCostoActivePipe();
    expect(pipe).toBeTruthy();
  });
});
