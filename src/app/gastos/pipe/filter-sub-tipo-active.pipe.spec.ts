import { FilterSubTipoActivePipe } from './filter-sub-tipo-active.pipe';

describe('FilterSubTipoActivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubTipoActivePipe();
    expect(pipe).toBeTruthy();
  });
});
