import { TotalImpuestosPipe } from './total-impuestos.pipe';

describe('TotalImpuestosPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalImpuestosPipe();
    expect(pipe).toBeTruthy();
  });
});
