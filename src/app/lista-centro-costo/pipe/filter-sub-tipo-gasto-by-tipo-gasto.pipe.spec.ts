import { FilterSubTipoGastoByTipoGastoPipe } from './filter-sub-tipo-gasto-by-tipo-gasto.pipe';

describe('FilterSubTipoGastoByTipoGastoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubTipoGastoByTipoGastoPipe();
    expect(pipe).toBeTruthy();
  });
});
