import { FiltraTiposPipe } from './filtra-tipos.pipe';

describe('FiltraTiposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraTiposPipe();
    expect(pipe).toBeTruthy();
  });
});
