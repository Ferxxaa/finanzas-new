import { SortAgnosPipe } from './sort-agnos.pipe';

describe('SortAgnosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAgnosPipe();
    expect(pipe).toBeTruthy();
  });
});
