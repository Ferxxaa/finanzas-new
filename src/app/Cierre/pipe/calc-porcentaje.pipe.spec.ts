import { CalcPorcentajePipe } from './calc-porcentaje.pipe';

describe('CalcPorcentajePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcPorcentajePipe();
    expect(pipe).toBeTruthy();
  });
});
