import { ConcatHistoricosPipe } from './concat-historicos.pipe';

describe('ConcatHistoricosPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatHistoricosPipe();
    expect(pipe).toBeTruthy();
  });
});
