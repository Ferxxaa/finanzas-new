import { FilterReportAreaNegocioPipe } from './filter-report-area-negocio.pipe';

describe('FilterReportAreaNegocioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportAreaNegocioPipe();
    expect(pipe).toBeTruthy();
  });
});
