import { OrdenesEvaluadasAgnoPipe } from './ordenes-evaluadas-agno.pipe';

describe('OrdenesEvaluadasAgnoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenesEvaluadasAgnoPipe();
    expect(pipe).toBeTruthy();
  });
});
