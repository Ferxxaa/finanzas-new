import { CortarTextosPipe } from './cortar-textos.pipe';

describe('CortarTextosPipe', () => {
  it('create an instance', () => {
    const pipe = new CortarTextosPipe();
    expect(pipe).toBeTruthy();
  });
});
