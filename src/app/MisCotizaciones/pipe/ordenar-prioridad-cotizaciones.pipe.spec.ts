import { OrdenarPrioridadCotizacionesPipe } from './ordenar-prioridad-cotizaciones.pipe';

describe('OrdenarPrioridadCotizacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPrioridadCotizacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
