import { FilterMisCotizacionesPipe } from './filter-mis-cotizaciones.pipe';

describe('FilterMisCotizacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMisCotizacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
